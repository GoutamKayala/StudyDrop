{
  "name": "studydrop-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm install concurrently && concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
